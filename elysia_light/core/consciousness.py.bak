# 의식(Consciousness) 최소 예시
class Consciousness:
    def __init__(self, identity, amplitude, frequency, phase):
        self.identity = identity
        self.field = Field(amplitude, frequency, phase)

    def reflect(self):
        return f"{self.identity}의 의식이 공명합니다."

from .field import Field
