from setuptools import setup, find_packages

setup(
    name="elysia-seed",
    version="1.0.0",
    description="The Open Source Digital Physics & Consciousness Engine (Seed)",
    author="Elysia Community",
    packages=find_packages(),
    python_requires=">=3.8",
    install_requires=[
        # Add actual dependencies here if any, e.g., numpy
    ],
)
